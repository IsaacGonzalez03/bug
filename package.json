{
  "name": "bug",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run setup:server",
    "setup:app": "npm run setup:server && npm run setup:client",
    "setup:client": "cd bug.client && npm i",
    "setup:server": "cd bug.server && npm i",
    "start": "node bug/index.js"
  },
  "engines": {
    "node": "^14.17.0"
  }
}